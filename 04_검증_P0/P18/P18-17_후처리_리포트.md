# P18-17 후처리 리포트

> 작성일: 2026-03-13  
> 대상: `assets/generated/` 전체 1,137장

---

## 1. 후처리 파이프라인 상태

### 도구
- **스크립트**: `tools/art-pipeline/post_process_pipeline.py`
- **의존성**: `Pillow`, `rembg`, `onnxruntime` 필요

### 실행 결과
- ❌ **실행 불가** — `Pillow` (PIL) 미설치, `rembg` 미설치
- 원인: Mac Mini 환경에 Python 이미지 처리 라이브러리가 설치되어 있지 않음
- 해결: `pip install Pillow rembg onnxruntime` 후 재실행 가능

---

## 2. 후처리 필요 항목

| 카테고리 | 수량 | 배경 제거 필요 | 사유 |
|---------|------|:---:|------|
| characters | 75 | ✅ | 투명 배경 스프라이트 |
| monsters | 214 | ✅ | 투명 배경 스프라이트 |
| cosmetics | 150 | ✅ | 아이콘/장비 투명 배경 |
| environment | 153 | ❌ | 배경 포함 의도 |
| ui | 335 | ❌ | 아이콘/HUD는 배경 제거 불필요 |
| vfx | 210 | ❌ | 이펙트 시트는 합성용 |

**배경 제거 대상**: 439장 (characters 75 + monsters 214 + cosmetics 150)

---

## 3. 현재 상태 요약

- 전체 1,137장 PNG 생성 완료, 깨진 파일 0건
- 후처리(배경 제거)는 의존성 설치 후 별도 실행 필요
- 환경/UI/VFX는 후처리 불필요 — 현재 상태로 사용 가능

### 후처리 실행 명령 (의존성 설치 후)
```bash
pip install Pillow rembg onnxruntime
python tools/art-pipeline/post_process_pipeline.py \
  --input assets/generated/characters \
  --output assets/processed/characters \
  --steps remove_bg
python tools/art-pipeline/post_process_pipeline.py \
  --input assets/generated/monsters \
  --output assets/processed/monsters \
  --steps remove_bg
python tools/art-pipeline/post_process_pipeline.py \
  --input assets/generated/cosmetics \
  --output assets/processed/cosmetics \
  --steps remove_bg
```

---

**판정**: 후처리 파이프라인 미실행 (의존성 미충족). 문서 기록 완료.
