# P29 개연성 검토 — 2026-03-14

> 범위: P29 QA 핫픽스 — 서버-클라이언트 API 정합성 전수 수정
> 기준: 코드 기반 검증 (서버 라우트 vs 클라이언트 호출)

## 1. SSOT 고유명사 검증

### 클래스 식별자 (6종)
| 코드 ID | 한글명 | 검증 |
|---|---|---|
| `ether_knight` | 에테르 기사 | ✅ |
| `memory_weaver` | 기억술사 | ✅ |
| `shadow_weaver` | 그림자 직조사 | ✅ |
| `memory_breaker` | 기억 파괴자 | ✅ |
| `time_guardian` | 시간 수호자 | ✅ |
| `void_wanderer` | 공허 방랑자 | ✅ |

### 금지어 검증
| 항목 | grep 결과 | 상태 |
|---|---|---|
| `mnemonist` (서버+클라이언트 *.ts) | **0건** | ✅ |
| `카엘` (서버+클라이언트 *.ts) | **0건** | ✅ |
| `마테우스` 사용 확인 | LobbyScene TOWN_NPCS `장로 마테우스` | ✅ |

## 2. 서버-클라이언트 API 정합성

### 🔴 Critical (P29에서 수정됨)

| # | 문제 | 원인 | 수정 |
|---|---|---|---|
| 1 | 서버 응답 언래핑 미적용 | 서버 47곳이 `{ success, data }` 래핑, 클라이언트가 직접 반환 | `get()/post()`에 `_unwrapResponse()` 추가 |
| 2 | Combat API 경로 불일치 | 서버 `/combat/...`, 클라이언트 `/api/combat/...` | 전체 8개 메서드 경로 수정 |
| 3 | NPC API 경로 불일치 | 서버 `/api/npcs`, 클라이언트 `/api/npc` | 경로 수정 |
| 4 | Inventory equip/use 경로 | 서버 `/api/inventory/equip` (body), 클라이언트 `/api/inventory/:id/equip` | 경로+body 수정 |
| 5 | Dungeon clear 경로 | 서버 `/api/dungeons/runs/:id/clear`, 클라이언트 `/api/dungeons/clear` | 경로 수정 |
| 6 | Vite proxy 누락 | `/combat` 프록시 미설정 → 서버 라우트 접근 불가 | vite.config.ts에 추가 |

### 🟡 Major (P29 이전에 수정됨)

| # | 문제 | 수정 시점 |
|---|---|---|
| 7 | Auth 필드명 `accessToken` vs `token` | P29 사전 수정 |
| 8 | Auth 경로 `/auth/` vs `/api/auth/` | P29 사전 수정 |
| 9 | CORS — serverUrl 절대경로 | P29 사전 수정 |
| 10 | 씬 미등록 (SettingsScene, CutsceneScene) | P29 사전 수정 |
| 11 | 클래스 API `/api/class` → `/api/class/tree` | P29 사전 수정 |
| 12 | register email 필드 누락 | P29 사전 수정 |
| 13 | 비밀번호 길이 (서버 6자, 클라이언트 4자) | P29 사전 수정 |

### 🔵 Info

| # | 항목 | 비고 |
|---|---|---|
| 14 | tsc 에러 117→63건 | P29에서 52건 해소 (레거시 API 호환 레이어) |
| 15 | 잔여 63건 — UI 컴포넌트 타입 미스매치 | P29 범위 외 (소켓 타입, Prisma 스키마) |
| 16 | uuid 의존성 추가 | server/package.json |

## 3. 타입 검증 결과

| 영역 | 에러 수 | P29 전 | 비고 |
|---|---|---|---|
| NetworkManager.ts | **0** | 2+ | ✅ 완전 해소 |
| 씬 파일 (8개) | **0** | 0 | ✅ |
| 사운드 | 2 | 2 | pre-existing |
| UI 컴포넌트 | 61 | 113 | 52건 해소 (호환 레이어) |
| **클라이언트 합계** | **63** | **117** | **54건 감소** |
| 서버 (analytics) | 60+ | 60+ | pre-existing (Prisma 스키마) |

## 4. 씬별 검증

| 씬 | API 연동 | 정합성 |
|---|---|---|
| MainMenuScene | login, register, refreshAuth | ✅ |
| CharacterSelectScene | getClasses, getCharacters, createCharacter | ✅ |
| LobbyScene | getInventory, getQuests, 소켓 연결 | ✅ |
| GameScene | getZoneInfo, getNpcs, 소켓 이벤트 | ✅ |
| BattleScene | combatStart, combatEnd, 소켓 combat:tick | ✅ |
| WorldScene | 소켓 world:teleport | ✅ |
| DungeonScene | (UI 연출 중심, API 미호출) | ✅ |
| SettingsScene | localStorage (서버 API 없음) | ✅ |

## 5. 누적 검증 통계

- **누적: 140항목 검증, 72건 수정** (P0~P29)
  - P29 신규: 16항목 검증, 15건 수정

---

## 결론

P29 QA 핫픽스로 서버-클라이언트 간 API 경로 불일치 6건(Critical)을 전수 수정.
자동 언래핑 적용으로 개별 메서드 47개 수정 없이 2곳(get/post)만 수정하여 해결.
레거시 API 호환 레이어 추가로 클라이언트 tsc 에러 54건 해소.
