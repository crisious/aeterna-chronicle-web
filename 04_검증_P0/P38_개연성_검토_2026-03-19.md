# P38 개연성 검토 — 미해결 이슈 전수 수정 (2026-03-19)

## 검토 범위
- GameScene 존 이동 에러 근본 수정
- TODO/FIXME 전수 해소
- E2E 테스트 전수 통과
- tsc 0에러 유지 검증
- SSOT 금지어/클래스명 정합성

---

## 🔴 Critical (0건)
없음.

## 🟡 Major — 수정 완료 (3건)

### M-1: GameScene preload 138개 오디오 404 → 씬 진입 불가
- **원인**: `SoundManager.preloadAll()`이 soundManifest 138개 오디오를 모두 `this.load.audio()`로 등록하지만, `client/public/assets/sounds/` 디렉터리 자체가 존재하지 않음 → 138개 전부 404
- **Phaser 동작**: loaderror 핸들러가 등록되어 있어 개별 404는 무시되지만, 대량 404 발생 시 로더 큐 처리 지연 + 일부 환경에서 preload→create 전환 시점 불안정
- **수정**: SoundManager 생성 + preloadAll()을 try-catch로 감싸 실패 시에도 씬 진입 보장
- **검증**: tsc 0에러, 브라우저 콘솔에서 경고만 출력, GameScene 정상 진입

### M-2: 프로그래매틱 텍스처 생성이 preload에서 실행
- **원인**: `this.add.graphics()`는 preload 단계에서 호출 시 Phaser 내부 상태에 따라 불안정
- **수정**: `_ensureFallbackTextures()` 메서드로 분리, create() 초입에서 호출
- **검증**: tsc 0에러

### M-3: LobbyScene TODO 1건 잔존
- **원인**: `// TODO: ShopUI 통합 후 교체` — 실제로는 인라인 ShopPanel이 이미 구현됨
- **수정**: TODO를 설명 주석으로 교체 → TODO/FIXME 전수 스캔 0건
- **검증**: `grep -rn 'TODO\|FIXME' server/src/ client/src/ --include='*.ts'` → 0건

## 🔵 Info (4건)

### I-1: SSOT 클래스명 정합성
- 코드 내 클래스 참조 404건 — 모두 SSOT 6종(`ether_knight`, `memory_weaver`, `shadow_weaver`, `memory_breaker`, `time_guardian`, `void_wanderer`)과 일치
- `mnemonist` 0건, `카엘` 0건 — 금지어 위반 없음

### I-2: E2E 테스트
- 17파일, 92테스트, 0실패
- unit + contract + e2e + admin-dashboard 전체 통과

### I-3: tsc 타입 검사
- 서버: 0에러
- 클라이언트: 0에러

### I-4: 오디오 에셋 현황
- soundManifest: 138개 (BGM 42 + SFX 75 + Voice 20 + Ambient 1)
- 실제 파일: 0개 (`client/public/assets/sounds/` 미존재)
- 현재 상태: preload 시 전부 404 → loaderror 핸들러가 무시 → 게임 무음으로 정상 동작
- 후속: 오디오 파일 생성/배포는 별도 Phase에서 처리

---

## 검증 요약
| 항목 | 결과 |
|------|------|
| GameScene 존 이동 | ✅ preload 방어 + fallback 텍스처 분리 |
| TODO/FIXME | ✅ 0건 |
| tsc 서버 | ✅ 0에러 |
| tsc 클라이언트 | ✅ 0에러 |
| E2E 테스트 | ✅ 92/92 통과 |
| SSOT 클래스명 | ✅ 6종 일치 (404참조) |
| 금지어 | ✅ mnemonist 0, 카엘 0 |

**누적: 395+ 항목 검증, 184+ 건 수정 (P0~P38)**
