# P30 E2E 테스트 결과 (2026-03-14)

## 실행 환경
- Vitest 3.2.4, Node v25.6.1 (darwin-arm64)
- 서버: localhost:3000 (ts-node-dev), PostgreSQL + Redis
- 클라이언트: Vite dev (localhost:5173)

## 테스트 결과 요약

| 구분 | 파일 수 | 통과 | 실패 | 통과율 |
|------|---------|------|------|--------|
| E2E (총계) | 24 | 22 | 2 | 91.7% |
| 테스트 케이스 | 199 | 186 | 13 | 93.5% |

## 통과 파일 (22/24)
auth, admin, analytics, auction, codex, craft, dungeon, economy, guild, inventory, monster, notification, npc, pet, pvp, quest, ranking, skill, tutorial, 외 3개

## 실패 파일 (2/24)

### 1. chapter1.test.ts — 9건 실패
- **원인**: 라이브 서버 통합 테스트 — 실제 DB 시딩 + 인증 필요
- **증상**: 회원가입 → 401 (서버 인증 정책) → 후속 테스트 cascading 실패
- **수정 사항**: API 경로 전수 교정 (P30)
  - `/character/create` → `/api/characters`
  - `/world/enter-zone` → `/api/world/zones/:code` + `/api/world/move`
  - `/quest/accept` → `/api/quests/:id/accept`
  - `/combat/start` → `/combat/start` (prefix 분리)
  - `/dungeon/enter` → `/api/dungeons/enter`
  - `/shop/buy` → `/api/shop/purchase`
- **대응**: DB 시딩 후 재실행 또는 테스트 유저 사전 생성 필요

### 2. social.test.ts — 4건 실패
- **원인**: 라이브 서버 + DB 데이터 의존
- **증상**: 거래/경매/친구/우편 — 유효 캐릭터 ID 없음 → 400/500
- **수정 사항**: API 경로 교정
  - `/party/accept` → `/party/invite/accept`
  - `/guild/create` → `/guild` (POST)
  - `/social/friend/add` → `/friends/request`
  - `/chat/send` → 소켓 기반 (REST 없음, 스킵 처리)
  - `/auction/search` → `/auction` (GET)

## P30 수정 사항

### vitest.config.ts
- JSDoc 내 glob 패턴이 esbuild 파싱 에러 유발 → 주석 형식 변환
- `resolve.alias` 추가: fastify, jsonwebtoken → server/node_modules

### analytics.test.ts
- `expectStatus(res, 200)` → `expectStatus(res.statusCode, 200)` (6건)
- `...authHeader()` → `headers: authHeader()` (전체)

## 결론
- 구조 테스트(mock/inject): **100% 통과**
- 라이브 서버 테스트: DB 시딩 후 재검증 필요 (경로 정합은 완료)
