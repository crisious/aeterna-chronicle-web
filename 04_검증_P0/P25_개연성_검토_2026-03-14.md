# P25 개연성 검토 리포트

- **검토일**: 2026-03-14
- **Phase**: P25 — 클라이언트-서버 연동 + 에셋 로딩
- **검증 도구**: grep, tsc --noEmit

---

## 1. SSOT 위반 검증

### 1.1 mnemonist 사용 금지
- **검색 범위**: `client/src/`, `server/src/`
- **결과**: 0건 ✅
- **판정**: PASS

### 1.2 카엘 사망 → 마테우스 교체
- **검색 범위**: `client/src/`, `server/src/`
- **카엘 언급**: 0건 ✅
- **마테우스 사용**: LobbyScene.ts의 NPC "장로 마테우스" — 정상
- **판정**: PASS

### 1.3 클래스 ID 정합성
- **SSOT 클래스**: `ether_knight`, `memory_weaver`, `shadow_weaver`, `memory_breaker`, `time_guardian`, `void_wanderer`
- **client/src/ 내 사용**: 20건 — 모두 SSOT 일치
- **비표준 ID 검출** (swordmaster, warrior, archer, mage 등): 0건 ✅
- **판정**: PASS

---

## 2. 코드-기획 매핑 검증

### 2.1 NetworkManager API ↔ 서버 라우트 매핑

| NetworkManager 메서드 | 서버 라우트 파일 | 일치 |
|---|---|---|
| combatStart → POST /api/combat/start | combatRoutes.ts | ✅ |
| combatAction → POST /api/combat/action | combatRoutes.ts | ✅ |
| combatTick → POST /api/combat/tick | combatRoutes.ts | ✅ |
| combatGetState → GET /api/combat/state/:id | combatRoutes.ts | ✅ |
| combatEnd → POST /api/combat/end | combatRoutes.ts | ✅ |
| combatLog → GET /api/combat/log/:id | combatRoutes.ts | ✅ |
| combatReplay → GET /api/combat/replay/:id | combatRoutes.ts | ✅ |
| combatActive → GET /api/combat/active | combatRoutes.ts | ✅ |
| login → POST /auth/login | authRoutes.ts | ✅ |
| register → POST /auth/register | authRoutes.ts | ✅ |
| getCharacters → GET /api/characters | characterRoutes.ts | ✅ |
| createCharacter → POST /api/characters | characterRoutes.ts | ✅ |
| getClasses → GET /api/class | classRoutes.ts | ✅ |
| getQuests → GET /api/quests | questRoutes.ts | ✅ |
| getInventory → GET /api/inventory/:id | inventoryRoutes.ts | ✅ |
| getZoneInfo → GET /api/world/zones/:id | worldRoutes.ts | ✅ |
| getNpcs → GET /api/npc | npcRoutes.ts | ✅ |

### 2.2 씬 ↔ NetworkManager 연결 상태

| 씬 | import networkManager | API 호출 | 소켓 이벤트 |
|---|---|---|---|
| MainMenuScene | ✅ | login, register, refreshAuth | — |
| CharacterSelectScene | ✅ | getCharacters, createCharacter, getClasses | — |
| LobbyScene | ✅ | getInventory, getQuests | world:playerJoined, system:notification |
| GameScene | ✅ | getZoneInfo | world:move, world:playerJoined/Left, monsterSpawn/Despawn |
| BattleScene | ✅ | combatStart, combatEnd | combat:tick, combat:result, combat:effectApplied |
| WorldScene | ✅ | — | world:teleport |
| DungeonScene | ✅ | — | — (서버 세션 ID 관리) |

---

## 3. 에셋 경로 검증

### 3.1 아틀라스 파일 존재
- `client/public/assets/atlas/characters.{png,json}` ✅
- `client/public/assets/atlas/effects.{png,json}` ✅
- `client/public/assets/atlas/ui.{png,json}` ✅

### 3.2 생성 에셋 심볼릭 링크
- `client/public/assets/generated → ../../../assets/generated` ✅
- `client/public/assets/resources → ../../../unity-client/Assets/Resources` ✅

### 3.3 에셋 규모
- 오디오 파일: 137개 (BGM 42 + SFX 75 + Voice 20)
- 이미지 파일: 1,383개

---

## 4. TypeScript 빌드 검증
- `npx tsc --noEmit` → **에러 0건** ✅

---

## 5. P25 변경 파일 목록

| 파일 | 변경 유형 |
|---|---|
| client/src/network/NetworkManager.ts | 전면 확장 (Auth, Combat, Quest, Inventory, Zone, Dungeon API + JWT + 재접속) |
| client/src/scenes/MainMenuScene.ts | 전면 재작성 (로그인/회원가입 UI + 자동 로그인) |
| client/src/scenes/CharacterSelectScene.ts | 전면 재작성 (서버 캐릭터 목록 + 생성 API) |
| client/src/scenes/LobbyScene.ts | 전면 재작성 (소켓 연결 + 퀘스트/인벤토리 API) |
| client/src/scenes/GameScene.ts | 전면 재작성 (존 정보 + NPC/몬스터 + 소켓 이벤트) |
| client/src/scenes/BattleScene.ts | 확장 (전투 API + 소켓 이벤트 + 서버 동기화) |
| client/src/scenes/WorldScene.ts | 확장 (텔레포트 소켓) |
| client/src/scenes/DungeonScene.ts | 확장 (서버 세션 ID) |
| client/src/scenes/LoadingScene.ts | 신규 (로딩 화면 + 진행률 바 + 팁) |
| client/src/assets/AssetManager.ts | 신규 (에셋 경로 매핑 + 캐싱 + placeholder) |

---

## 6. 검증 요약

| 항목 | 건수 | 결과 |
|---|---|---|
| SSOT 위반 (mnemonist) | 0 | ✅ PASS |
| SSOT 위반 (카엘) | 0 | ✅ PASS |
| 비표준 클래스 ID | 0 | ✅ PASS |
| API 매핑 불일치 | 0 | ✅ PASS |
| 씬 NetworkManager 미연결 | 0 | ✅ PASS |
| tsc 빌드 에러 | 0 | ✅ PASS |
| 에셋 경로 누락 | 0 | ✅ PASS |

**누적 검증**: 131항목, 73건 수정 (P25에서 추가 검증 7항목, 수정 0건)
