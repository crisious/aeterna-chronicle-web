# 에테르나 크로니클 — P1-07 텔레메트리 스키마 구현결과 v1

> 작성일: 2026-03-06  
> 기준 스키마: `03_데이터테이블/npc_choice_event_telemetry_schema_v1.md`

---

## 1) 구현 범위

### Client (Phaser)
- `client/src/telemetry/dialogueTelemetry.ts` 추가
  - 이벤트 타입 정의
  - `buildDialogueChoiceTelemetry()` 빌더 구현
  - `idempotencyKey = sessionId:dialogueNodeId:choiceId` 생성
- `client/src/scenes/GameScene.ts` 연동
  - 대화 선택 확정 시 텔레메트리 생성
  - 소켓 연결 시 `telemetry:dialogue_choice` emit
  - 오프라인 시 콘솔 fallback 로깅

### Server (Socket)
- `server/src/telemetry/dialogueTelemetryServer.ts` 추가
  - payload 검증
  - idempotency dedupe(5분 윈도우, 메모리 맵)
  - `telemetry:ack` 응답(`ok`, `deduped`, `idempotencyKey`)
- `server/src/socket/socketHandler.ts` 연동
  - `telemetry:dialogue_choice` 이벤트 핸들러 추가

---

## 2) 검증

- Client 타입체크: `npx tsc --noEmit` 통과
- Server 빌드: `npm run build` 통과

---

## 3) 잔여 작업

- [ ] Prisma/Redis 기반 영속 저장소 연결
- [ ] 대시보드 KPI 집계 파이프라인 연결
- [ ] Unity/UE5 런타임 동일 빌더 포맷 적용

---

## 4) 판정

**P1-07 구현 1차 완료 (Done 기준 충족)**
- 스키마 정의 + 클라이언트/서버 이벤트 경로 + dedupe/ack 최소 요건 충족.
