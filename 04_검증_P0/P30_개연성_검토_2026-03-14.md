# P30 개연성 검토 (2026-03-14)

## SSOT 검증

### 1. 클래스 6종 ✅
- `ether_knight`, `memory_weaver`, `shadow_weaver`, `memory_breaker`, `time_guardian`, `void_wanderer`
- CharacterSelectScene.ts FALLBACK_CLASSES: 6종 일치
- 서버 classRoutes.ts: 6종 일치
- SkillTreeUI.ts CLASS_NAMES: 6종 일치

### 2. mnemonist 금지 ✅
- 전체 코드 검색: 0건 (client/src, server/src)
- `memory_weaver` 통일 확인

### 3. 카엘 사망 ✅
- 전체 코드 검색: 0건
- LobbyScene.ts NPC: "장로 마테우스" 확인

### 4. TODO/FIXME ✅
- 전체 검색: 0건 (SPEED_HACK 제외)
- ShopUI TODO 해결: `_executeTrade` → `/api/shop/purchase`, `/api/inventory/sell`
- SkillTreeUI TODO 해결: `characterLevel` 실제 값 전달

## API 경로 정합성 검증

### 수정된 경로 (5건)
| 파일 | 변경 전 | 변경 후 | 서버 매칭 |
|------|---------|---------|-----------|
| ShopUI.ts | TODO (미연결) | POST /api/shop/purchase, /api/inventory/sell | ✅ |
| SkillTreeUI.ts | POST /api/skills/:id/upgrade | POST /api/skills/levelup | ✅ |
| DialogueUI.ts | GET /api/dialogue/:npcId | POST /api/dialogue/start | ✅ |
| socialRoutes.ts | (미노출) | POST /api/friends/reject 추가 | ✅ |
| chapter1.test.ts | /character/create 등 | /api/characters 등 | ✅ |

### 정합 확인 (12건)
- auth: `/api/auth/register`, `/login`, `/refresh`, `/logout`, `/me` ✅
- character: `/api/characters`, `/api/characters/:id` ✅
- inventory: `/api/inventory/:userId`, `/sell`, `/equip` ✅
- quest: `/api/quests`, `/api/quests/:id/accept`, `/complete` ✅
- world: `/api/world/zones`, `/zones/:code`, `/move` ✅
- combat: `/combat/start`, `/action`, `/apply-effect` ✅
- shop: `/api/shop/items`, `/purchase` ✅
- skill: `/api/skills/:userId`, `/levelup`, `/equip` ✅
- dungeon: `/api/dungeons`, `/enter` ✅
- npc: `/api/npcs`, `/npcs/:id` ✅
- class: `/api/class` ✅
- dialogue: `/api/dialogue/start`, `/choose`, `/history` ✅

### Vite Proxy ✅
- `/api` → localhost:3000 ✅
- `/auth` → localhost:3000 ✅ (사용되지 않으나 무해)
- `/combat` → localhost:3000 ✅
- `/socket.io` → localhost:3000 (ws) ✅

## 소켓 이벤트 매핑 검증

| 클라이언트 이벤트 | 서버 핸들러 | 상태 |
|-------------------|-------------|------|
| world:move | worldSocketHandler | ✅ |
| world:teleport | worldSocketHandler | ✅ |
| world:playerJoined | worldSocketHandler broadcast | ✅ |
| world:playerLeft | worldSocketHandler disconnect | ✅ |
| world:monsterSpawn | worldSocketHandler | ✅ |
| world:monsterDespawn | worldSocketHandler | ✅ |
| combat:tick | combatManager broadcast | ✅ |
| combat:result | combatManager broadcast | ✅ |
| combat:effectApplied | combatManager broadcast | ✅ |
| chat:message | chatSocketHandler | ✅ |
| guild:join/leave/chat | guildSocketHandler | ✅ |
| auction:subscribe | auctionSocketHandler | ✅ |
| trade:update/complete | tradeSocketHandler | ✅ |
| party:update/join | partySocketHandler | ✅ |
| pvp:matchFound/result | pvpSocketHandler | ✅ |
| system:notification | systemBroadcast | ✅ |

## 씬 전환 데이터 체인 ✅

| 전환 | 전달 데이터 | 상태 |
|------|-------------|------|
| MainMenu → CharSelect | (없음, 인증 상태) | ✅ |
| CharSelect → Lobby | characterId, characterName, characterClass, className, baseStats, level | ✅ |
| Lobby → WorldScene | characterData (LobbySceneData) | ✅ |
| Lobby → DungeonScene | characterData | ✅ |
| GameScene → BattleScene | zoneId, monsterId, monsterName | ✅ |
| BattleScene → GameScene | (씬 전환만) | ✅ |

## 누적: 129항목 검증, 78건 수정
