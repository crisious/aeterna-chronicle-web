# ── 클라이언트 프로덕션 환경변수 ────────────────────────────
# Docker 빌드 시 자동 적용 (vite build --mode production)

# 프로덕션 서버 URL (배포 시 실제 도메인으로 교체)
VITE_SERVER_URL=https://api.aeterna-chronicle.com
VITE_WS_URL=wss://api.aeterna-chronicle.com
